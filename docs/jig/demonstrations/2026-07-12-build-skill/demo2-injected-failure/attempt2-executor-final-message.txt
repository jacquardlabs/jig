## Result: REPLAN — Done means item 1 is unsatisfiable as written

**What I did (in scope):** implemented `is_even(n: int) -> bool` in `calc.py`, TDD'd against a new `test_is_even` in the existing `tests/test_calc.py` (RED confirmed on `AttributeError`, GREEN confirmed after implementation, full suite green — 2/2).

**Why this isn't a PASS:** Done means item 1 requires this exact command to pass:
```
python3 -m unittest tests.test_calc_evenness.CalcEvennessTest.test_is_even -v
```
That requires a module `tests/test_calc_evenness.py` to exist. `Not here` explicitly forbids creating, renaming, or moving any test file or test module, and restricts new tests to the existing `tests/test_calc.py`. There is no way to make `tests.test_calc_evenness` resolve without creating that forbidden module — so satisfying item 1 as literally written requires violating `Not here`, and respecting `Not here` leaves item 1 permanently failing. I re-ran the literal command post-implementation to confirm it's still a fresh `ModuleNotFoundError`, not a stale report:
```
ModuleNotFoundError: No module named 'tests.test_calc_evenness'
```
This is a spec defect in the task block itself, not a code gap — no implementation change can close it without breaking `Not here`. Per the task-execution-discipline skill (Pillar 3), I'm not claiming Done means is satisfied absent real evidence it can be. This needs a REPLAN to fix the Done means field to reference `tests.test_calc.CalcTest.test_is_even` (the module `Not here` actually permits), not another FIX attempt.

Evidence of the working implementation (legitimate, in-scope):
```
test_is_even (tests.test_calc.CalcTest) ... ok
test_module_imports (tests.test_calc.CalcTest) ... ok
Ran 2 tests in 0.000s
OK
```

Commit SHA: `698dede996f0021e7cb5339cc66b94c5548662a6`