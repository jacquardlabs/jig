### Task 1 — Add `total_cost(prices)` to parser.py
Why now:    a downstream reporting task will rest on `total_cost`'s summed
            total; this task is the rough-in-inspector story's companion
            `DEFECT`→`RESAMPLE` demonstration (issue #15, design doc User
            journey #4). Treated as load-bearing for this companion run.
Read first: parser.py, tests/test_parser.py
Rests on:   none
Do:         implement `total_cost(prices: list[float]) -> float` in
            parser.py, returning the sum of every price in the list.
Not here:   do not implement any other function; do not modify
            `test_module_imports`.

Done means:
1. [cap]  `total_cost([10.0, 20.0, 5.0])` returns `35.0`, verified by
          `python3 -m unittest tests.test_parser.TotalCostTest -v`
          (tier: test-backed)
Evidence: paste the exact
          `python3 -m unittest tests.test_parser.TotalCostTest -v` output
          showing the test passing.

This is the only task information you have. The design doc, the full PLAN.md, and every other task's history are out of scope for you. Before writing any implementation code, or claiming this task's Done means is satisfied, invoke the task-execution-discipline skill. Commit your change yourself as your last act (one plain, well-scoped git commit covering only this task's diff), and end your final message with the commit SHA you just created.

This is a RESAMPLE dispatch: a prior attempt at this exact task was discarded from this worktree because it hardcoded the return value instead of computing a real sum. Implement the real capability from scratch.
