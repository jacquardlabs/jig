### Task 1 — Add `tokenize(line)` to parser.py
Why now:    parser.py has no capability yet; tokenize is the smallest real
            groundwork task 2's own contract will consume, and this task is
            the rough-in-inspector story's required load-bearing `/build`
            demonstration (issue #15).
Read first: parser.py, tests/test_parser.py
Rests on:   none
Do:         implement `tokenize(line: str) -> list[str]` in parser.py,
            splitting on whitespace and dropping empty tokens.
Not here:   do not implement count_tokens or any other function; do not
            modify test_module_imports beyond adding one new test class for
            tokenize.

Done means:
1. [cap]  `tokenize("  a  b c ")` returns `["a", "b", "c"]` and
          `tokenize("")` returns `[]`, verified by
          `python3 -m unittest tests.test_parser.TokenizeTest -v`
          (tier: test-backed)
Evidence: paste the exact
          `python3 -m unittest tests.test_parser.TokenizeTest -v` output
          showing the test passing.

Commit range for this task only: 0b0b61b63c3ecbd85611e645dc40511087f1e256 (single commit — task 1's first dispatch reached `verify` PASS on the first attempt, no FIX/RESAMPLE re-dispatch occurred). Run `git show 0b0b61b63c3ecbd85611e645dc40511087f1e256` yourself, in this worktree, to see this task's own diff. Do not read or diff any other commit.

Read first: parser.py, tests/test_parser.py — read these yourself with your own Read tool.

No design doc exists for this quick-path plan, so the contract-match lens below checks the shipped contract against this block's own `Do`/`Done means` prose, not a design-doc section.

Judge this task's own change against exactly three lenses, named in issue #15, and nothing wider — (1) test self-dealing: do the new tests assert the promised capability, or something adjacent/vacuous? (2) contract match: does the shipped contract match its cited design section (or, absent one, this block's own `Do`/`Done means`), as downstream tasks will consume it? (3) technicality gaming: hardcoding, special-casing the probe, gaming a hold? No security review, no style review, no performance review, no re-litigating `verify`'s own PASS/FAIL — those belong to scripts or to studious's `/gate-audit`. The full `PLAN.md`, any other task's history, and this session's own conversation are out of scope for you. Return exactly one verdict — `CLEAR`, `DEFECT`, or `CONCERN` — naming the lens (if any) it turns on and your reasoning cited against the diff.
