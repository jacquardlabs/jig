---
name: design
description: STUB — not yet implemented (jig milestone M2, see PRODUCT.md's critical user journeys and DESIGN.md's Vocabulary table). Will become jig's /design skill — batch interview, forked options, a sectioned design doc, and a viva review, emitting the DESIGNED | NEEDS RESEARCH | REVISED verdict per the ratified handoff (§5.1). Do not invoke for actual design work yet; there is no behavior behind this file.
---

# /design (stub)

This is a scaffold placeholder, not a working skill. It exists so `skills/`
has the directory shape jig's later milestones implement into (M1 —
Repo & plugin scaffold), per this repo's own acceptance criteria.

When M2 implements `/design`, this file becomes the real entry point:
batch interview → forks → sectioned design doc → viva review, per the
critical user journey in `PRODUCT.md` and the verdict vocabulary in
`DESIGN.md`.
