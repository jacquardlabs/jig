---
name: finish
description: STUB — not yet implemented (jig milestone M5/M6, see PRODUCT.md's critical user journeys and DESIGN.md's Vocabulary table). Will become jig's /finish skill — a PR evidence table (Done-means → verification method → evidence link → pass), a cctx harvest, follow-ups, and cleanup, emitting a MERGE | PR | KEEP | DISCARD verdict per the ratified handoff (§5.4). Do not invoke for actual finish work yet; there is no behavior behind this file.
---

# /finish (stub)

This is a scaffold placeholder, not a working skill. It exists so `skills/`
has the directory shape jig's later milestones implement into (M1 —
Repo & plugin scaffold), per this repo's own acceptance criteria.

When a later milestone implements `/finish`, this file becomes the real
entry point: PR evidence table, cctx harvest, follow-ups, and cleanup, per
the critical user journey in `PRODUCT.md` and the verdict vocabulary in
`DESIGN.md`.
