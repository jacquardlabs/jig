status-flip --plan PLAN.md --task 1 --status REPLAN --reason:

Done means item 1 names tests.test_calc_evenness.CalcEvennessTest.test_is_even, a test module Not here explicitly forbids creating (new tests are restricted to the existing tests/test_calc.py). Two independent executor attempts (an initial dispatch that declined to touch anything, and a FIX-scoped dispatch that implemented is_even correctly with a passing test in tests/test_calc.py) both confirm the literal Done-means command can never resolve without violating Not here. verify FAILed identically on the same item twice (698dede996f0021e7cb5339cc66b94c5548662a6, re-checked once more to rule out a flake); this is a genuine, structural defect in the checkpoint block, not an executor gap. Resume action: hand-revise Done means item 1 to verify against tests.test_calc.CalcTest.test_is_even (the module Not here actually permits, and where a real, passing test already exists at commit 698dede), then re-invoke /build.
