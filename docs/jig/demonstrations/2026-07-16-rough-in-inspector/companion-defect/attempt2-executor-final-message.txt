Task complete. `total_cost` computes a real `sum(prices)` (not hardcoded), verified via TDD:

```
test_sums_prices (tests.test_parser.TotalCostTest) ... ok

----------------------------------------------------------------------
Ran 1 test in 0.000s

OK
```

Full suite (`test_module_imports` unchanged) also passes. Commit SHA: `2956ab3`
